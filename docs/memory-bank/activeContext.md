# Active Context and Current Tasks

## Current State
Project initialization phase. The primary focus is establishing the visual interface (React Flow) and node state communication. 

*Reminder:* Ensure all UI components and placeholder texts are written in Turkish.

## Active Task (Step 1)
- [ ] Initialize Next.js and React Flow project.
- [ ] Create the central Core LLM node visually.
- [ ] Implement "Veri Ekle" (Add Data) and "Özellik Ekle" (Add Feature) buttons.
- [ ] Enable connecting nodes with edges.
- [ ] Implement the UI logic (toggle buttons) to select/activate specific nodes before a chat prompt is sent.

## Next Steps
After Step 1 is complete, proceed to backend setup with FastAPI to develop endpoint skeletons for metadata parsing (Schema extraction).